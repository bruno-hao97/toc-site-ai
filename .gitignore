node_modules/
dist/
dist-server/
data/
*.tsbuildinfo
.env
.env.local
.env*.local
*.env.local
*.db

# FTP deploy (local credentials + staging)
deploy/ftp.local.ps1
deploy/ftp-upload/
deploy/bridge-upload/

# Reference-only Next.js project at repo root (has its own git history)
/landing/
